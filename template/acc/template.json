{
  "contest": {
    "dir": "{{ contest.id }}"
  },
  "task": {
    "dir": "{{ task.id | lower }}",
    "program": [
      "main.rs",
      "main.cpp",
      "main.py"
    ],
    "submit": "{task.program}"
  }
}
